name: shopdekho_customer
description: ShopDekho — Local Shops, Fresh Vegetables (Customer App)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  provider: ^6.1.2
  geolocator: ^12.0.0
  qr_code_scanner_plus: ^2.0.10
  url_launcher: ^6.3.0
  share_plus: 13.0.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
